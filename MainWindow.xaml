<Window x:Class="MyShopProject.MainWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
                Title="MainWindow" Height="1000" Width="1198"
                xmlns:local="clr-namespace:MyShopProject"
        SizeToContent="WidthAndHeight">

    <Window.Resources>
        <local:Base64ToBitmapConverter x:Key="bitmapConverter"/>
        <local:PriceConverter x:Key="priceConverter"/>
    </Window.Resources>
    <Canvas Height="1000" Width="1198" >
        <telerik:RadTabControl Height="968" Width="1199" SelectionChanged="tabChanged" telerik:StyleManager.Theme="Material" HorizontalAlignment="Center" VerticalAlignment="Top" >
            <telerik:RadTabItem x:Name="RadTabItem4" Header="Dashboard"/>
            <telerik:RadTabItem x:Name="RadTabItem5" Header="Category" >
                <Canvas Margin="0,0,0,-372" >
                    <telerik:RadDataForm 
                        ItemsSource="{Binding listCat}"
                        x:Name ="currentCat"
                        telerik:StyleManager.Theme="Office2019" 
                        Height="388" Width="1178" Canvas.Top="538" HorizontalAlignment="Left" VerticalAlignment="Top" AutoGeneratingField="categoryGenerated2" DeletingItem="beforeDelCat" Canvas.Left="11"/>

                    <telerik:RadGridView 
                        ItemsSource="{Binding listCat}"
                        ShowGroupPanel="False"
                        x:Name="listCategory" 
                        telerik:StyleManager.Theme="Office2019" 
                        HorizontalAlignment="Center" Canvas.Top="10" 
                        VerticalAlignment="Center" Width="1178" Height="458" 
                        Canvas.Left="11" RowHeight="35"  ColumnWidth="*" IsReadOnly="True" SelectedCellsChanged="selectCateEvent"/>
                    <telerik:HtmlDataProvider RichTextBox="{Binding ElementName=radRichTextBox}"/>

                </Canvas>
            </telerik:RadTabItem>
            <telerik:RadTabItem x:Name="RadTabItem6" Header="Product">
                <Grid Background="#00000000" Margin="0,0,0,-356">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="155*"/>
                        <ColumnDefinition Width="35*"/>
                        <ColumnDefinition Width="1009*"/>
                    </Grid.ColumnDefinitions>
                    <telerik:RadCardView 
                        ItemsSource="{Binding listBook}"
                         CardHeaderBinding="{Binding Name}"
                        IsReadOnly ="True"
                        MajorLength = "373"
                        x:Name ="bookCardView"
                        HorizontalAlignment="Left" 
                        VerticalAlignment="Top"
                        AutoGenerateDataFieldDescriptors="False"
                        telerik:StyleManager.Theme="Office2019" Grid.ColumnSpan="3" Width="1178" Height="577" Margin="10,10,0,0">

                        <telerik:RadCardView.DataFieldDescriptors>
                            <telerik:CardDataFieldDescriptor Header="Book cover:" DataMemberBinding="{Binding ImageBase64}">
                                <telerik:CardDataFieldDescriptor.ReadOnlyTemplate>
                                    <DataTemplate>
                                        <Border VerticalAlignment="Top" HorizontalAlignment="Left" CornerRadius="13" >
                                            <Image 
                                            VerticalAlignment="Top" HorizontalAlignment="Left"
                                            Source="{Binding ImageBase64, Converter={StaticResource bitmapConverter}}"
                                            Height ="100"
                                            Width ="100"/>
                                        </Border>
                                    </DataTemplate>
                                </telerik:CardDataFieldDescriptor.ReadOnlyTemplate>
                            </telerik:CardDataFieldDescriptor>
                            <telerik:CardDataFieldDescriptor Header="ID:"  DataMemberBinding="{Binding Id}" />
                            <telerik:CardDataFieldDescriptor Header="Book name:" DataMemberBinding="{Binding Name}" />
                            <telerik:CardDataFieldDescriptor Header="Category:" DataMemberBinding="{Binding Name}" />
                            <telerik:CardDataFieldDescriptor Header="Author:" DataMemberBinding="{Binding Author}" />
                            <telerik:CardDataFieldDescriptor Header="Published year:" DataMemberBinding="{Binding PublishedYear}"/>
                            <telerik:CardDataFieldDescriptor Header="Purchase price:" DataMemberBinding="{Binding PurchasePrice , Converter={StaticResource priceConverter}}"/>
                            <telerik:CardDataFieldDescriptor Header="Selling price:" DataMemberBinding="{Binding SellingPrice, Converter={StaticResource priceConverter}}"/>
                            <telerik:CardDataFieldDescriptor Header="Quantity:" DataMemberBinding="{Binding Quantity}"/>
                            <telerik:CardDataFieldDescriptor Header="Description:" DataMemberBinding="{Binding Description}">
                                <telerik:CardDataFieldDescriptor.ReadOnlyTemplate>
                                    <DataTemplate>
                                        <TextBlock VerticalAlignment="Top" HorizontalAlignment="Left" TextWrapping="Wrap" Width ="215" Text="{Binding Description}">

                                        </TextBlock>
                                    </DataTemplate>
                                </telerik:CardDataFieldDescriptor.ReadOnlyTemplate>
                            </telerik:CardDataFieldDescriptor>
                        </telerik:RadCardView.DataFieldDescriptors>

                        <telerik:RadCardView.CardHeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontWeight="Bold"/>
                            </DataTemplate>
                        </telerik:RadCardView.CardHeaderTemplate>

                    </telerik:RadCardView>
                    <telerik:RadDataPager 
                        Source="{Binding listBook}"
                        DisplayMode="All" HorizontalAlignment="Left" Margin="11,592,0,0" VerticalAlignment="Top" telerik:StyleManager.Theme="Material" Height="45" Width="1178" PageSize="1" Grid.ColumnSpan="3"/>
                </Grid>
            </telerik:RadTabItem>

        </telerik:RadTabControl>

    </Canvas>
</Window>
